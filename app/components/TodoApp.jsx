var React = require('react');

var TodoApp = React.createClass({
    render: function() {
        return(
            <div>
                Todo root app
                </div>
        )
    }
});

module.exports = TodoApp;